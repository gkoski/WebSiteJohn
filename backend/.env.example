# Copie para .env (ou exporte como variaveis de ambiente) e preencha com valores reais.
# Nunca commite o .env de verdade.

DB_URL=jdbc:mysql://localhost:3306/mrjohn_db?createDatabaseIfNotExist=true
DB_USERNAME=root
DB_PASSWORD=

# Gere um valor forte e aleatorio, nunca reaproveite este exemplo.
JWT_SECRET=troque-por-um-valor-aleatorio-forte
